import 'package:flutter/material.dart';

class Wrongbar extends StatelessWidget {
  const Wrongbar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}